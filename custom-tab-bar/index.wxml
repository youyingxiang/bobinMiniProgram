<van-tabbar active="{{ active }}" bind:change="onChange">
  <van-tabbar-item wx:for="{{ list }}"  wx:key="key" wx:for-item='item' icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
